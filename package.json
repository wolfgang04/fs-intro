{
  "name": "fs-intro",
  "version": "1.0.0",
  "scripts": {
    "prestart": "kill-port 6062",
    "start": "concurrently \"npm start --prefix client\" \"npm start --prefix server\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "kill-port": "^2.0.1"
  },
  "dependencies": {
    "react-router": "^6.26.0",
    "react-router-dom": "^6.26.0"
  }
}
